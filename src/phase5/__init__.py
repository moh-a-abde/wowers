"""Phase 5 — ML model on labeled ground truth.

Modules
-------
ground_truth : ingest + normalize labeled hydropower data (energy output per
               plant) into the canonical ground-truth schema consumed by the
               Phase 5 training pipeline.
"""
